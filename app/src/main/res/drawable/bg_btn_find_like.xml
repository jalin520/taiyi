<?xml version="1.0" encoding="UTF-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- 填充的颜色 -->
    <solid android:color="@color/bg_layout" />
    <stroke
        android:width="@dimen/c_divide_line_horizontal_height"
        android:color="@color/bg_divide_line" />

    <!-- 设置矩形的四个角为弧形 -->
    <!-- android:radius 弧形的半径 -->
    <corners android:radius="@dimen/item_shop_cart_good_out_time_btn_find_like_radius" />

</shape>